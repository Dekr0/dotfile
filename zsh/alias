gac() {
	git add . & git commit -m "update"
}

#mount-drive() {
#    sudo mount -t ntfs-3g "$1" /mnt/win
#}

#install-discord() {
#	sudo tar xzf $1 -C /opt
#	sudo ln -s /opt/Discord/Discord /usr/bin/discord
#}

fork() {
	(setsid "$@" &)
}

alias cp="cp -i"                                                # Confirm before overwriting something
alias df="df -h"                                                # Human-readable sizes
alias free="free -m"                                            # Show sizes in MB

#alias mountE="sudo ntfs-3g /dev/sda1 /mnt/windows/E"
#alias mountF="sudo ntfs-3g /dev/sda2 /mnt/windows/F"
#alias mountG="sudo ntfs-3g /dev/sda3 /mnt/windows/G"
#alias mBackup="sudo mount /dev/sdc1 /mnt/backup"

#alias umountE="sudo umount /mnt/windows/E"
#alias umountF="sudo umount /mnt/windows/F"
#alias umountG="sudo umount /mnt/windows/G"
#alias uBackup="sudo umount /mnt/backup"

eval "$(dircolors -b)"
alias ls="ls --color=auto"

alias b2="b2-linux"
alias br="chromium"
#alias vivado="/home/dekr0/Xilinx/Vivado/2020.1/bin/vivado"

alias list-drive="lsblk -f"
alias fixSwing="wmname LG3D"
#alias unmount-drive="sudo umount /mnt/win"
#alias monitor-normal="xrandr --output HDMI-0 --rotate normal"
#alias monitor-right="xrandr --output HDMI-0 --rotate right"
alias shutdown="shutdown now"
